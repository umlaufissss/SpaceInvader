class Grid
{
    constructor(rows, cols)
    {
        this.rows = rows;
        this.cols = cols;
    }
    
    init()
    {
        const array = []

        for (let row = 0; row < array.l)
    }
}

export default Grid;