class Grid
{
    constructor(rows, cols)
    {
        this.rows = rows;
        this.cols = cols;
    }
    
    init()
    {
        const array = []

        for (let row = 0; row < array.length; row++)
        {
            for
        }
    }
}

export default Grid;