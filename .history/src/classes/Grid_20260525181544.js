class Grid
{
    constructor(rows, cols)
    {
        this.rows = rows;
        this.cols = cols;
    }
    
    init()
    {
        const array = []

        for (let row = 0; row < this.ro; row++)
        {
            for(let cols = 0; cols < array.length;cols++)
            {

            }
        }
    }
}

export default Grid;